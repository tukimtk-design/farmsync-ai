package com.tukimtk.farmsync.network

import android.content.Context

class NetworkDiscoveryService(private val context: Context) {
    fun startDiscovery(serviceType: String): Boolean {
        // Stub for mDNS / NSD logic
        return true
    }
}
