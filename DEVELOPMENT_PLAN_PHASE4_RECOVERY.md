# แผนพัฒนา Phase 4: การแก้ไขข้อผิดพลาด (Post-Mortem Recovery) และการพัฒนาเชิงลึก

จากการทบทวนการทำงานในเฟสที่ผ่านมา พบว่าเกิดข้อผิดพลาดรุนแรงในการสร้างโค้ดลักษณะ "เปลือกเปล่า (Empty Stubs)" เพื่อให้ผ่านการคอมไพล์ โดยไม่ได้เขียนลอจิกการทำงานจริง ทำให้ไม่สามารถใช้งานฟีเจอร์ที่วางแผนไว้ใน Phase 2 และ Phase 3 ได้ (เช่น mDNS, SAF, Localization)

เพื่อป้องกันไม่ให้แอปพลิเคชันเป็นเพียงแค่หน้ากาก (UI Shells) เฟสนี้จะเป็นการกลับไป **"เขียนโค้ดจริงทีละฟีเจอร์"** โดยยึดหลักเกณฑ์ความเสถียรและใช้งานได้จริงเป็นอันดับแรก

## 1. แนวทางการแก้ไข (Corrective Approach)
*   **เลิกทำ Master Tasks:** จะไม่มีการรวมฟีเจอร์ใหญ่ 5 อย่างเข้าด้วยกันใน PR เดียวอีกต่อไป การพัฒนาจะถูกแบ่งออกเป็นโมดูลเล็กๆ ที่ทำงานได้จริง (Functional PRs)
*   **ห้ามใช้ Stubs เป็นงานสำเร็จ:** งานที่ส่งมอบจะต้องไม่ใช่ `// TODO` หากฟีเจอร์ใดซับซ้อนเกินไป จะต้องใช้เวลาในการเขียนลอจิก (เช่น การอ่านไฟล์ผ่าน InputStream ใน Android, การใช้ NsdManager) ให้สมบูรณ์ก่อน
*   **ผสานดีไซน์จาก Phase 3:** แม้จะเน้นลอจิก แต่ UI จะต้องถูกสร้างขึ้นตามคอนเซปต์ "Gamified UI" ที่วางแผนไว้ ไม่ใช่แค่โครงสร้างดิบๆ

## 2. แผนการทำงานเชิงลึกแบบแบ่งย่อย (Incremental Implementation Plan)

### Step 4.1: ระบบ Localization และ UI โครงสร้างพื้นฐาน (Foundation)
*   สร้าง `LanguageManager.kt` ของจริงที่มี Map ข้อมูลภาษาไทย/อังกฤษครบถ้วน
*   แก้ไข `MainActivity.kt` ให้มี 5 Tabs สมบูรณ์ (เพิ่ม Save Editor) พร้อมไอคอน Material 3 แบบเต็มรูปแบบ
*   *เป้าหมาย:* ให้ได้ UI ที่สวยงามตาม Phase 3 โดยไม่มี Hardcoded Strings

### Step 4.2: ระบบจัดการไฟล์ที่ทำงานได้จริง (Functional Storage & SAF)
*   นำ Stub SAF ใน `StorageSettingsScreen.kt` ออก และเขียนโค้ดที่สามารถรับ URI จากระบบ Android, คัดลอกไฟล์ด้วย `ContentResolver`, และโยนงานเข้า `Dispatchers.IO` ได้อย่างสมบูรณ์
*   ผูกระบบเข้ากับ `SnackbarHostState` เพื่อแสดงผลลัพธ์ให้ผู้ใช้เห็นทันที
*   *เป้าหมาย:* ผู้ใช้ต้องกดเลือกไฟล์ .zip และแอปสามารถอ่านข้อมูลได้จริง

### Step 4.3: ระบบโครงข่าย mDNS ตัวจริง (True Auto-Discovery)
*   แก้ไข `NetworkDiscoveryService.kt` ให้เลิก Return แบบหลอกๆ
*   เขียนโค้ดฝั่ง Android ฝัง `android.net.nsd.NsdManager` เพื่อทำการเปิด Broadcast (Register Service) และค้นหาเครื่อง PC (Discover Service)
*   *เป้าหมาย:* แอปสามารถส่งและรับ Packet หาสัญญาณ Wi-Fi ได้จริงเพื่อเตรียมการ Sync แบบ AirDrop-like

### Step 4.4: AI Assistant และ Save Editor ลอจิก
*   เขียนโค้ดแปลงข้อมูล `SaveMetadata` ลงในหน้า UI
*   นำข้อมูลจาก `StardewGlossary` ไปใช้งานร่วมกับ AI Prompts ได้จริง
*   *เป้าหมาย:* ฟีเจอร์ Killer Features ต้องทำงานได้จริง และมี UI ตอบสนอง ไม่ใช่เพียงแค่คลาสเปล่า

## บทสรุป
Phase 4 นี้ไม่ใช่การวางแผนฟีเจอร์ใหม่ แต่เป็นการ **"ทำสิ่งที่รับปากไว้ใน Phase 1-3 ให้เป็นความจริง"** โดยเน้นคุณภาพโค้ดระดับ Production-ready และป้องกันปัญหา Scope Overload ที่ทำให้เกิดข้อผิดพลาดในอดีต
