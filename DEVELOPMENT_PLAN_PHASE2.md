# แผนพัฒนา Phase 2: ยกระดับ UI/UX และปรับปรุงประสิทธิภาพ (Optimization)

เป้าหมายหลักในเฟสนี้คือการสร้างประสบการณ์ผู้ใช้ (UX) ที่ลื่นไหล ส่วนต่อประสาน (UI) ที่สวยงามสะอาดตา และเพิ่มประสิทธิภาพของโค้ดให้ทำงานรวดเร็วและไร้ข้อผิดพลาด

## 1. UI/UX Polish (ความสวยงามและใช้งานง่าย)
- **ระบบ Theming:** ออกแบบ `Theme.kt` ใหม่ทั้งหมดโดยใช้ชุดสี (Color Palette) ที่ดูสบายตา (Clean & Minimal) และรองรับทั้ง Light/Dark mode
- **ไอคอนและเมนู:** นำ `androidx.compose.material.icons` มาใช้ใน Navigation Bar ทุกจุด พร้อมทำแอนิเมชันตอนกด (Ripple Effect & Transitions) เพื่อเพิ่มความน่าใช้งาน
- **สถาปัตยกรรม UI:** แยกคอมโพเนนต์ออกเป็นส่วนย่อยๆ (Reusable Composables) เพื่อให้โค้ดดูสะอาดตาและง่ายต่อการดูแล
- **สถานะการโหลด:** เพิ่ม `CircularProgressIndicator` หรือ Shimmer Effect ขณะรอการซิงค์ไฟล์ เพื่อไม่ให้หน้าจอดูกระตุกหรือค้าง

## 2. Code Optimization & Performance (ความเร็วและเสถียรภาพ)
- **การจัดการ Coroutine ที่ถูกวิธี:** ย้ายลอจิกที่กินทรัพยากรสูง (เช่น การอ่าน/เขียนไฟล์, SAF, เครือข่าย mDNS) ไปไว้ที่ `Dispatchers.IO` เพื่อรักษา UI ให้ทำงานที่ 60 FPS เสมอ
- **Error Handling ขั้นสูง:** หุ้มกระบวนการทำงานที่มีความเสี่ยงด้วย `try-catch` อย่างละเอียด และส่งแจ้งเตือนกลับไปยัง `SnackbarHostState` อย่างสวยงาม (ไม่ปล่อยให้แอปเด้งออกเด็ดขาด)
- **Memory Management:** เลิกใช้โค้ดที่สร้าง Object ซ้ำซ้อน และเปลี่ยนไปใช้กลไก `remember` หรือ `derivedStateOf` ใน Compose อย่างมีประสิทธิภาพ

## 3. การจัดการภาษา (Localization)
- สร้างระบบ `LanguageManager` เพื่อแยกข้อความ (Strings) ทุกจุดออกจากโค้ด UI รองรับการสลับภาษา (ไทย-อังกฤษ) แบบทันที (Dynamic) ทำให้ UI สะอาดตาและดูเป็นมืออาชีพ

## 4. แผนการทดสอบ
- เขียน Unit Tests เพิ่มเติมสำหรับ `LanguageManager` และ Flow การดักจับ Error
- รันและทดสอบบนตัวจำลอง (Emulator) เพื่อยืนยันว่าไม่มีจุดใดทำงานหน่วง (Jank)
